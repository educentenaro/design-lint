{
  "name": "design-lint",
  "version": "0.1.0",
  "description": "AST-based React design token linting for JavaScript, TypeScript and CSS",
  "engines": { "node": ">=22" },
  "files": ["dist"],
  "type": "module",
  "bin": {
    "design-lint": "./dist/cli.js"
  },
  "scripts": {
    "start": "node ./dist/cli.js",
    "test": "npm run build && node --test tests/unit/*.test.js tests/integration/*.test.js tests/e2e/*.test.js",
    "test:package": "node scripts/test-package.mjs",
    "check": "tsc --noEmit",
    "build": "tsc",
    "prepack": "npm run check && npm run build"
  },
  "dependencies": {
    "@babel/parser": "^7.26.10",
    "@babel/traverse": "^7.26.10",
    "css-tree": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
